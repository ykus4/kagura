# Generated by CMake from lit.site.cfg.py.in — do not edit.
#
# Two-stage generation: configure_file() expands the at-sign placeholders,
# then file(GENERATE) resolves the target-file generator expression for the
# plugin path, which configure_file() cannot do on its own.
#
# Keep this file free of literal generator expressions, including inside
# comments — file(GENERATE) evaluates them wherever they appear.

config.opt = "@LLVM_TOOLS_BINARY_DIR@/opt"
config.filecheck = "@FILECHECK_EXECUTABLE@"
config.kagura_plugin = "@KAGURA_PLUGIN_PATH@"
config.kagura_src_root = "@CMAKE_CURRENT_SOURCE_DIR@/lit"
config.kagura_obj_root = "@CMAKE_CURRENT_BINARY_DIR@/lit"

lit_config.load_config(config, "@CMAKE_CURRENT_SOURCE_DIR@/lit/lit.cfg.py")
